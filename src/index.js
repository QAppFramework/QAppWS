/**
 * QApp — PWA runner/wrapper for native-feeling web app experiences.
 *
 * JS tooling entry point. Qt/QML handles the UI; this module will provide
 * URL validation, manifest fetching, and install logic in later phases.
 */

/** Application name constant. */
export const APP_NAME = 'QApp';
